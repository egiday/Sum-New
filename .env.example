# Database connection string
DATABASE_URL="postgresql://user:password@localhost:5432/fastpoll?schema=public"

# Example production database URL (when using Vercel Postgres)
# DATABASE_URL="postgres://default:password@host:port/verceldb?sslmode=require"